open Generator 

(* TODO 3: Migrate graphics_sandbox to this, and update the types.  *)
let generate_map w h n : map = {border = {points : [||]; sides [||]};
                                regions = [] }
